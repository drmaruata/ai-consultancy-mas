"""
IP Registry module.

Manages reusable IP assets (templates, accelerators, blueprints) across the MAS.
"""

from ip_registry.registry import IPAsset, IPRegistry

__all__ = ["IPAsset", "IPRegistry"]
